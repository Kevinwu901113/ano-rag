# Core dependencies
numpy>=1.21.0,<2.0.0
pandas>=1.3.0
scipy>=1.7.0
scikit-learn>=1.0.0

# FAISS - GPU version preferred, fallback to CPU
# Using faiss-gpu-cu12 for Python 3.12 compatibility
faiss-gpu-cu12>=1.7.4; platform_machine=="x86_64"
faiss-cpu>=1.7.4; platform_machine!="x86_64"

# Deep Learning and Transformers
transformers>=4.20.0
torch>=1.12.0
sentence-transformers>=2.2.0

# NVIDIA RAPIDS for GPU acceleration (CUDA 12.x compatible)
# Note: These GPU dependencies are automatically installed on x86_64 systems
# If you encounter CUDA compatibility issues, you may need to install manually:
# pip install cudf-cu12 cuml-cu12 cugraph-cu12 cupy-cuda12x
cudf-cu12>=23.10.0; platform_machine=="x86_64"
cuml-cu12>=23.10.0; platform_machine=="x86_64"
cugraph-cu12>=23.10.0; platform_machine=="x86_64"
cupy-cuda12x>=12.0.0; platform_machine=="x86_64"

# Graph processing
networkx>=2.8

# Document processing
python-docx>=0.8.11
jsonlines>=3.0.0

# LLM integration
ollama>=0.5.1
openai>=1.0.0
requests>=2.28.0

# Clustering and embeddings
umap-learn>=0.5.3
hdbscan>=0.8.28

# NLP and NER
spacy>=3.4.0
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.12.0  # Optional but recommended for better performance

# Utilities
tqdm>=4.64.0
pyyaml>=6.0
loguru>=0.6.0
jsonschema>=4.0.0
GPUtil>=1.4.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Evaluation
datasets>=2.0.0
