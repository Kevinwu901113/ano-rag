include:
  - ../manifests/datasets.yaml
  - ../manifests/methods.yaml

exp:
  name: audit_min_check
  goal: "Minimal audit for acceptance"

meta:
  result_root: result_relrag/miniaudit

llm_profiles:
  - name: default
    model: qwen3-30b-a3b
    endpoint: http://127.0.0.1:8000/v1

datasets:
  - hotpotqa_distractor_200

methods:
  - dense_rag

embedding:
  model: /home/wjk/models/qwen3-emb
  device: cpu

runtime:
  limit: 5
  workers: 1
  resume: false
  save_every: 5
  run_eval: true

decode:
  temperature: 0.0
  max_tokens: 200

topk:
  retrieve_k: 5
  gen_k: 5
