include:
  - common.yaml
  - ../manifests/datasets.yaml
  - ../manifests/methods.yaml

exp:
  name: e1_cache_smoke
  goal: "Cache smoke test for dense RAG"
  llm_profile: default

datasets:
  - hotpotqa_distractor_200

methods:
  - dense_rag

report:
  budgets:
    token_budgets: [4096, 8192]

runtime:
  limit: 20

meta:
  result_root: result_relrag/e1_cache_smoke
